# NQ 期货数据 · Databento 导出

用你的 API key 拉的原始数据 + 我算出来的衍生序列。**数据是你的**（你付的费，共 $18.83）。

## 文件

| 文件 | 内容 | 行数 | 大小 |
|---|---|---|---|
| `NQ_v_0_1m_2010_2026.parquet` | **原始 1 分钟 OHLCV**，Databento 直出 | 4,806,377 | 73 MB |
| `NQ_v_0_5m_with_ema12_atr14.parquet` | 我聚合的 5 分钟 + EMA12 + ATR14 | 989,324 | 32 MB |

两个文件都是 zstd 压缩的 parquet。读法：

```python
import pandas as pd
df = pd.read_parquet("NQ_v_0_1m_2010_2026.parquet")
```

## 原始数据的请求参数

这些是复现这批数据需要的全部信息：

| 参数 | 值 |
|---|---|
| `dataset` | `GLBX.MDP3` |
| `symbols` | **`NQ.v.0`** |
| `stype_in` | `continuous` |
| `schema` | `ohlcv-1m` |
| `start` | `2010-06-06`（GLBX.MDP3 数据集起点） |
| `end` | `2026-08-29`（不含） |

实际覆盖：`2010-06-07 00:00 UTC` → `2026-08-28 20:59 UTC`，约 **16.2 年**。

### ⚠ 为什么是 `.v.0` 而不是 `.c.0`

**这一条如果选错，整个回测就是虚构的。**

- `.c.0` = **日历排序**连续合约 —— 到期周仍然停在**将死的近月**上
- `.v.0` = **成交量排序**连续合约 —— 跟着实际主力走

我一开始用的 `.c.0`，在试点里发现了：**2024-09-20 那天 09:30 那一小时，
`.c.0` 只有 380 手，`.v.0` 有 68,278 手。** 在 380 手的合约上回测出来的成交价没有意义。

换成 `.v.0` 重拉，那次错误浪费了 $1.28。

## 字段

**1 分钟文件：**

| 列 | 类型 | 说明 |
|---|---|---|
| `ts` | `datetime64[ns, UTC]` | bar **开始**时间，UTC |
| `open` / `high` / `low` / `close` | `float64` | 价格，0.25 跳 |
| `volume` | `uint64` | 手数 |

**5 分钟文件**：同上，另加 —

| 列 | 说明 |
|---|---|
| `ema12` | EMA12，α = 2/13，用**前 12 根的 SMA 播种**（不是从第一根开始递推） |
| `atr14` | Wilder ATR14，α = 1/14，同样 SMA 播种 |

前 11 根的 `ema12` 是 `NaN`，前 13 根的 `atr14` 是 `NaN`——这是正确行为，不是缺数据。

### 5 分钟是怎么聚合的

按 **UTC 整点边界**（`origin="epoch"`）聚合，不是按 ET。

这样是安全的，因为纽约开盘 09:30 ET 在夏令时落在 13:30Z、冬令时落在 14:30Z，
两个都正好是 5 分钟边界。信号 bar 的选取是用 `zoneinfo` 按 `America/New_York`
的本地时间判断的，所以夏令时切换会被正确处理。

**指标跑在 24 小时连续时段上**，不是只有 RTH。这一点很重要：
RTH-only 的图表会算出**不同的 EMA**，从而给出不同的信号，而且不会报错。

## 数据质量：降级日

拉取过程中 Databento 警告了这些天为 `degraded`（该日数据质量下降）：

```
2014-06-11  2017-11-13  2018-10-21  2019-01-15
2020-02-27  2021-12-05  2022-01-02  2025-09-17
2026-01-31  2026-03-15  2026-03-16
```

**这是我从抓取日志里抄下来的，共 11 天，但这个清单可能不全**——
Databento 的警告信息本身是截断的（结尾是 `...`）。

我本想用 `metadata.get_dataset_condition`（免费）复查一次完整清单，
但 API key 已经不在这个环境里了（当初也建议过你轮换掉，因为它在对话里出现过）。
你想要准确清单的话自己跑一下：

```python
import databento as db
c = db.Historical("<你的 key>")
cond = c.metadata.get_dataset_condition(
    dataset="GLBX.MDP3", start_date="2010-06-06", end_date="2026-08-29")
print([r["date"] for r in cond if r["condition"] != "available"])
```

**我没有剔除这些天**——它们照常进入了回测。16.2 年里的十来天，
影响可以忽略，但你应该知道它们存在，而不是以为数据是完美的。

## 我没有做的事

- **没有做除权/连续合约的价差调整。** `.v.0` 在换月时价格会跳。
  对这个策略无所谓（每天独立开平，不跨月持仓），但如果你要做长期持有的研究，
  这个跳空必须先处理。
- **没有过滤任何异常价格。** 原样保留。
- **1 分钟文件是 Databento 直出**，我只做了按时间排序和跨年份合并
  （已断言时间单调递增、无重复）。

## 复现

```bash
export DATABENTO_API_KEY=<你的 key>
./.venv/bin/python scripts/fetch_data.py     # 有 $18 的花费上限保护
```

脚本按年缓存，已经拉过的年份不会重复付费。

---

数据版权归 Databento / CME 所有，按你的 Databento 订阅条款使用。
